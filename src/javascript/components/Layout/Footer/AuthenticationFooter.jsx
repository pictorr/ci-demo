import React, { PureComponent } from 'react';

// This can be used if you want to have a different footer for the authentication pages or other pages, just rename it accordingly
class AuthenticationFooter extends PureComponent {
	render() {
		return (
			<footer/>
		);
	}
}

export default AuthenticationFooter;